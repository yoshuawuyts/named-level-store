{
  "name": "named-level-store",
  "version": "1.0.0",
  "description": "Create a levelDB instance on your local machine",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps",
    "test:cov": "standard && npm run deps"
  },
  "repository": "yoshuawuyts/named-level-store",
  "keywords": [
    "leveldb",
    "local",
    "create"
  ],
  "license": "MIT",
  "dependencies": {
    "level": "^1.5.0",
    "mkdirp": "^0.5.1"
  },
  "devDependencies": {
    "dependency-check": "^2.6.0",
    "istanbul": "^0.4.5",
    "standard": "^8.5.0",
    "tape": "^4.6.2"
  }
}
